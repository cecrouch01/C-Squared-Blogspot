{{#each blogs }}
<div>
    <h1>{{ id }}</h1>
    <h2> {{title}} </h2>
    <h2> {{description}} </h2>
    <h2> {{user.username}} </h2>
    <h2> {{date_created}} </h2>
    <h2>{{logged_in}}</h2>
</div>
{{/each}}
